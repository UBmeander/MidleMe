<%= form_for(@midleme) do |f| %>
  <% if @midleme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@midleme.errors.count, "error") %> prohibited this midleme from being saved:</h2>

      <ul>
      <% @midleme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.number_field :location_id %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_field :longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
